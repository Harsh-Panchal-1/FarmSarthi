<!DOCTYPE html>
<html>
<head>
  <title>Farming Tips</title>
  <link rel="stylesheet" href="/css/tips.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Figtree:wght@400;600;700&display=swap">
</head>
<body>
  <h1>🌱 Farming Tips</h1>

  <% farming_tips.forEach(tip => { %>
    <div class="tip-card">
      <h3>
  <% if (tip.category === 'price') { %>📈<% } %>
  <% if (tip.category === 'soil') { %>🌱<% } %>
  <% if (tip.category === 'storage') { %>📦<% } %>
  <%= tip.title %>
</h3>

      <p><%= tip.content %></p>
      <small>
        Crop: <%= tip.crop %> |
        Category: <%= tip.category %> |
        Date: <%= tip.date %>
      </small>
    </div>
  <% }) %>

  <div class="pagination">
    <% if (currentPage > 1) { %>
      <a href="/tips?page=<%= currentPage - 1 %>">Previous</a>
    <% } %>

    <span>Page <%= currentPage %> of <%= totalPages %></span>

    <% if (currentPage < totalPages) { %>
      <a href="/tips?page=<%= currentPage + 1 %>">Next</a>
    <% } %>
  </div>
</body>
</html>
